export const Products = () => {
    return (
        <div>
            <div> this would be the top bar and contains buttons for adding more product </div>
            <div>
                this would be the table structure for products
            </div>
        </div>
    )
}